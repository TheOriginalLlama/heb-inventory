{{ include "heb-inventory.fullname" . }} has been deployed.

Access the API:

{{- if .Values.ingress.enabled }}
  http://{{ .Values.ingress.host }}{{ .Values.ingress.path }}
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "heb-inventory.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/healthz
{{- end }}

Verify pods:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}
