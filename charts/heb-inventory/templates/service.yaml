apiVersion: v1
kind: Service
metadata:
  name: {{ include "heb-inventory.fullname" . }}
  labels:
    {{- include "heb-inventory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "heb-inventory.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
